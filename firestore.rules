rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /submissions/{docId} {
      // Anyone can create a submission (opt-in research data)
      allow create: if true;
      // No client can read, update, or delete — only via Firebase Console / Admin SDK
      allow read, update, delete: if false;
    }
  }
}
